# Lazytype — Python-afhankelijkheden (Windows én macOS).
# Installeren:  python -m pip install -r requirements.txt
#
# Op macOS trekken pynput en pystray automatisch de benodigde pyobjc-frameworks mee.
# Tk (voor het key-venster) zit in de python.org-installer; bij Homebrew: `brew install python-tk`.

sounddevice>=0.5
pynput>=1.8
pyperclip>=1.11
requests>=2.32
pystray>=0.19
Pillow>=10
